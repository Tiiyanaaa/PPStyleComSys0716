PPStyleComSys0716.app Installation
----------------------------------

To install PPStyleComSys0716.app using zc.buildout and the plone.recipe.zope2instance
recipe to manage your project, you can do this:

* Add ``PPStyleComSys0716.app`` to the list of eggs to install, e.g.:

    [buildout]
    ...
    eggs =
        ...
        PPStyleComSys0716.app

* Re-run buildout, e.g. with:

    $ ./bin/buildout

